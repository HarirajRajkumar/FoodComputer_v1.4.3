/*unsigned long previousMillis_DHT = 0;

float DHT11Temp()
{
  float t ;
  unsigned long myDHTMillis = millis();
  if(myDHTMillis - previousMillis_DHT > 10)
  {
    previousMillis_DHT = myDHTMillis;
    // DHT11 
    // Read temperature as Celsius (the default)
  t= dht.readTemperature();
  }
  return t;
}// close 
*/
